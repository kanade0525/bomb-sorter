# Bomb Sorter の開発ルール

## 大原則

- **ドキュメント・コメント・テスト名・コミットメッセージはすべて日本語**で書く
- コメントは「何をしているか」ではなく「**なぜそうしたか**」「一度こう失敗した」を残す
- コミットメッセージは体言止めにせず、平叙の日本語文で書く
  - 良い例: `落ちてくるボムを色で仕分ける中核ルールを置く` / `iOS でドラッグが死ぬのを touch-action で直す`
- 書式は Prettier に任せる（セミコロンなし・シングルクォート・2スペース・100桁）

## 崩してはいけない設計の境界

### 1. `src/game/` と `src/core/` は純粋レイヤ

`window` / `document` / `navigator` / `localStorage` / `performance` / `Date` / `Math.random`
を**一切触らない**。乱数は `world.rng` を引数で持ち回り、音や描画は呼ばずに
`world.effects` へ「起きたこと」を積むだけにする。

これが守られている限り、ゲームのルール全体が Vitest だけで検証でき、
同じシードと同じ入力から必ず同じ結果が出る。`tools/check.mjs` が機械的に見張っている。

### 2. 数値は `src/core/constants.ts` に集める

難易度・座標・秒数・スコア式のパラメータを他のファイルへ直書きしない。散らすと調整不能になる。

### 3. 座標系の真実は `computeLayout` の返す `Layout` ひとつ

判定用と描画用の座標を二重に持たない。ここを分けると必ずずれる。

**縦持ちと横持ちの両方に対応する**。どちらでも短い方の辺を 360 に固定し、
長い方の辺だけ伸縮させる。向きによって変わるのは箱の位置だけ（縦は下に 2 つ、
横は左右の端）で、ボムの大きさ・当たり判定・難易度は変えない。
`computeFit` と `computeLayout` の向きの判定は必ず同じ基準にすること
（片方が横だと思っているのにもう片方が縦を返すと、座標がまるごとずれる）。
拡大率には上限（`FIELD.MAX_SCALE`）がある。**論理座標と CSS 座標を変換する計算を
テスト側に書くときは、必ずこの上限も同じように掛けること。**
一度これを忘れて、当たり判定の余裕のせいで「近いボムだけたまたま掴める」状態になり、
ずれていることに気づくのが遅れた。

### 3-b. 色は明度でも判別できる組み合わせを選ぶ

ボムすけは 2 種類とも完全に同じ姿で、違うのは色だけ。形での冗長化が無いぶん、
赤と黒という**コントラスト比 5.3:1** の組み合わせで、色が分からなくても
明暗で判別できる状態を保つ。`src/view/palette.test.ts` が 3:1 を下回らないか見張っている。
色を変えるときは必ずこのテストを通すこと。

### 4. `localStorage` に触るのは `src/platform/storage.ts` だけ

キーは `bomb-sorter:<なにか>:v<数字>` の形に揃える。読み書きは必ず try/catch で包む
（Safari のプライベートブラウズや容量枯渇で例外が飛ぶ）。

### 4-b. 見た目はピクセルアートで統一する

曲線・グラデーション・滑らかな線を混ぜない。1 か所でも混じると、そこだけ浮いて見える。

- 図形はドット（`BOMB.PIXEL` の倍数）で置く。角丸は `roundRect` ではなく、
  角をドット 1 個ぶんずつ削った多角形で作る
- 数字は `src/view/pixel-font.ts` の 5x7 字形で描く。日本語のラベルだけは
  システムフォントのまま（漢字を 5x7 で組むと読めなくなる）
- DOM の UI も、角丸ではなく `clip-path` で角を落とし、`box-shadow` の inset で
  ベベルを作る
- 破片は丸ではなく四角、リングは円弧ではなく円周に並べたドット

### 5. 外部から素材を持ってこない

音は Web Audio で合成、絵は Canvas に手続き的に描く、文字は `system-ui`。
音源・フォント・写真のバイナリをリポジトリに入れない。
本番ビルドの CSP `connect-src 'none'` を破らない。

### 6. `innerHTML` を使わない

DOM は要素を組み立てて作る。

## 検査を足す文化

新しい種類の不具合を見つけたら、直すだけでなく **`tools/check.mjs` に検査を足す**か、
**テストを足す**。同じ種類の失敗が二度起きない状態にしてから閉じる。

## push する前に

```bash
npm run verify   # check → build → e2e
```

E2E が落ちているものを公開しない。`deploy.yml` にも `npm run check` を入れてあるが、
手元で通してから出すのが原則。

## 見た目を変えたとき

```bash
npm run shots && open shots/
```

実機（iPhone Safari）でしか分からないこと（サイレントスイッチでの無音、
ホーム画面に追加したときの表示、ドラッグの感触）は必ず実機で確認する。

## 一度やって直した失敗（同じ轍を踏まないために）

- **誤った箱に × を出していたが、これは意図的に取りやめた**。落とす前に正誤が分かると
  「やば、こっちじゃない」と気づけてしまい、慌てて間違える瞬間そのものが無くなる。
  パニックゲームとして、箱の表示は「どこに落ちるか」だけを見せ、「合っているか」は見せない。
  かつて逆の方針（誤りの箱に × と暗幕）を実装していたので、戻すときは意図を確認すること
- **ドラッグの可動域の下端とゾーンの当たり判定の下端が 1px ずれていて**、画面下 50px が
  「入れたのに無反応」の死角になっていた。clamp の範囲と `containsPoint` の境界は必ず突き合わせる
- **画面の高さが変わった瞬間に、指を動かしていないのに誤爆死した**。座標系が変わったら
  進行中のドラッグは手放す（`releaseAllDrags`）
- **爆発の演出が常に round の見た目になっていた**。座標の一致でボムを探していたため。
  「起きたこと」は座標で引き当てず、必要な情報を Effect に持たせる
- **同一 pointerId で 2 個掴めた**（マウスの左押しっぱなし＋右クリック）。触っていないボムが判定された
- **スコアが 5 桁になると HUD のコンボ表示と重なった**。文字は「桁が増えても衝突しない」配置にする。
  Canvas の右上は DOM のボタンの領域なので `FIELD.HUD_RESERVED_RIGHT` を空ける
- **検分用の計測スクリプトが旧実装の数値を直書きしていて、直したのに「重なっている」と報告した**。
  実装をなぞる計測は、なぞっている旨をコメントに書き、実装を変えたら一緒に直す
- **置換で直したつもりが空振りしていた**ことが何度もある。直したら必ず撮り直して目で見る
- **E2E ヘルパが論理幅を決め打ちしていて**、横持ちに変えたら全部のドラッグが見当違いの場所を掴んだ。
  座標変換は必ず `layout` の実際の値と `FIELD.MAX_SCALE` から出す
- **縦持ちの寸法でテストを開始していて**、回転案内が出てゲームを始められずタイムアウトした
  （その後、縦持ちも遊べるようにしたので案内自体を廃止した）
- **向きに依存する当たり判定をコードに直書きしていた**。ドラッグの可動域を
  「左端の箱から右端の箱まで」と書いていたので、箱が下に並ぶ縦持ちで破綻しかけた。
  フィールドと箱をぜんぶ含む外接矩形にして、向きに依存しない形にした

## やらないこと

- 画像の差分比較（`toHaveScreenshot`）を CI のゲートにしない。Canvas とフォント描画の差で必ず不安定になる
- 難易度上昇に「色の見分けにくさ」を使わない。上げるのは同時数・間隔・導火線・歩く速さだけ
- ボムを落下させない。自分の足で歩かせる。触っていないボムが勝手に箱へ入ると理不尽になる
- UI 文言をひらがなだらけにしない。子供向けのゲームではないので、漢字を使う
- 序盤を「1 体ずつ運ぶだけ」にしない。数体から始めて短い間隔で湧かせる（`SPAWN.BURST_AT_START`）。
  ただし開始時点で大量に並べるのもだめ。一度 10 体置いてみたら、いきなり捌ききれない量が
  見えて「難しい」ではなく「もう無理」になり、手が止まった。増えていく過程を見せる
- Service Worker の `skipWaiting` を無条件に呼ばない。プレイ中に勝手にリロードされるとスコアが飛ぶ
